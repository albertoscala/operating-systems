// Struttura dati che definisce l'operazione
typedef struct {
    char operator;
    float first_value;
    float second_value;
} Operation;

// Struttura dati che definisce il risultato
typedef struct {
    long ricezione;
    long invio;
    float result;
} Response;
